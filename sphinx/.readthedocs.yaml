# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# Set the version of Python and other tools you might need
build:
  os: ubuntu-22.04
  tools:
    python: '3.11'
    # You can also specify other tool versions:
    # nodejs: "19"
    # rust: "1.64"
    # golang: "1.19"

  apt_packages:
    - python3-dev
    - libmysqlclient-dev
    - apache2
    - apache2-dev
    - libapache2-mod-wsgi-py3
    - make
    # - mysql-server

  jobs:
    post_checkout:
      - echo "Just a test, carry on ..."
    pre_build:
      - pip install python-dotenv
      - echo -e "MYSQL_HOST=localhostsql994.main-hosting.eu\nMYSQL_USER=u418907658_swobuser\nMYSQL_PASSWORD=@SMSWithoutBorders1234\nMYSQL_DATABASE=u418907658_swobdatabase" > sphinx/.env
      - cat sphinx/.env
      # - mysql -u root < sphinx/setup.sql

# Build documentation in the docs/ directory with Sphinx
sphinx:
  configuration: sphinx/docs/source/conf.py
# If using Sphinx, optionally build your docs in additional formats such as PDF
# formats:
#    - pdf

# Optnally declare the Python requirements required to build your docs
python:
  install:
    - requirements: requirements.txt
    - requirements: sphinx/requirements.txt
io